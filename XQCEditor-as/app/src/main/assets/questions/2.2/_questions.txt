问题描述
    输入：两个字符串，str和ends，
	输出：str是否以ends为后缀的判断结果。

输入格式
    首先输入被判断的字符串str  回车
	再输入指定的后缀字符串ends  回车

输出格式
    ture  结果为真  表示str是以ends为后缀的字符串
	false 结果为假  表示str不是以ends为后缀的字符串

数据规模与约定
    str  的最大长度为500个字符
	ends 的最大长度为50个字符